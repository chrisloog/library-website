<!DOCTYPE html>
<html lang="en">

	<head>
		<meta charset="UTF-8"/>
		<meta http-equiv="X-UA-Compatible" content="IE=edge"/>
		<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
		<title>Author form</title>
		<link rel="stylesheet" href="styles.css"/>
	</head>

	<body>
		<div id="container">
			<div class="header-footer">
				<a href="index.twig">Books</a>&nbsp;|&nbsp;
				<a href="book-add.twig">Add books</a>&nbsp;|&nbsp;
				<a href="author-list.twig">Authors</a>&nbsp;|&nbsp;
				<a href="">Add authors</a>
			</div>
			{% if msg is defined and msg == 'error' %}
				<div class="header-footer" id="error-message">
					Title should be between 3 and 23 characters!
				</div>
			{% endif %}

			<main>
				<section>
					<form id="input-form" method="post">
						<div class="label-cell">
							<label for="name">First name:</label>
						</div>
						<div class="input-cell">
							<input id="name" name="firstName" value="{{ firstName is defined ? firstName : '' }}" type="text"/>
						</div>

						<div class="label-cell">
							<label for="last-name">Last name:</label>
						</div>
						<div class="input-cell">
							<input id="last-name" name="lastName" value="{{ lastName is defined ? lastName : '' }}" type="text"/>
						</div>

						<div class="label-cell">Rating:</div>
						<div class="input-cell">
							<label>
								<input type="radio" name="grade" value="1" {{ rating is defined and rating == 1 ? 'checked' : '' }}/>1
							</label>
							<label>
								<input type="radio" name="grade" value="2" {{ rating is defined and rating == 2 ? 'checked' : '' }}/>2
							</label>
							<label>
								<input type="radio" name="grade" value="3" {{ rating is defined and rating == 3 ? 'checked' : '' }}/>3
							</label>
							<label>
								<input type="radio" name="grade" value="4" {{ rating is defined and rating == 4 ? 'checked' : '' }}/>4
							</label>
							<label>
								<input type="radio" name="grade" value="5" {{ rating is defined and rating == 5 ? 'checked' : '' }}/>5
							</label>
						</div>

						<div class="flex-break"></div>
						<div class="label-cell"></div>
						<div class="input-cell button-cell">
							<input name="submitButton" type="submit" formaction="functions.php?cmd=author-save" value="Salvesta"/>
						</form>
					</div>
				</section>
			</main>
			<div class="header-footer">
				Library Project
			</div>
		</div>
	</body>

</html>
